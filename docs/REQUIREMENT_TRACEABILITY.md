# Release 1 Requirement Traceability

## Completion rule

A requirement may be marked **Complete** only when its implementation, automated test or repeatable verification, documentation evidence, and visible demo evidence all exist and agree. Application requirements remain **Not Started** until Release 1 work begins.

| ID | Requirement | Rubric reference | Planned implementation | Automated test or repeatable verification | Documentation evidence | Visible demo evidence | Status |
|---|---|---|---|---|---|---|---|
| R1-01 | Create repository, issue, analysis, recommendation, human-decision, and audit-event schemas that can safely accommodate tenants and users later. | B: schema separation; H: tenant model and auditability | FastAPI domain models, SQLAlchemy models, Alembic migrations, and separate API schemas. | Locally verified Alembic upgrade/downgrade/re-upgrade on PostgreSQL and foreign-key, repository-scoped uniqueness, and non-null constraint tests. GitHub Actions CI #4 completed successfully for commit `f823911`. | ADR 0005, including its ER diagram, and the initial Alembic migration. | Inspectable issue and analysis records. | Complete |
| R1-02 | Import a selected public repository and cached fixture of approximately 100–300 issues. | B: bounded public import; G: sanitization | Bounded importer, public fixture set, provenance records, and sanitization policy. | PostgreSQL suite: 49 passed, 0 skipped. Offline/local suite: 37 passed, 12 PostgreSQL-dependent skips. Ruff format/lint, Alembic upgrade, and Compose validation passed. Fixture verification confirmed 100 unique sanitized issues; repeat import inserted 100, then 0 without duplicates. Pull-request and push CI passed for PR #2; post-merge main CI #13 passed for commit `8a50a82`. | ADR 0002, ADR 0006, README offline-import guide, and `backend/fixtures/pallets_flask/` manifest. | Offline import shows 100 issues after first and repeat runs. | Complete |
| R1-03 | Provide issue-list and issue-detail views with typed contracts and clear states. | A: workflow UI, typed contracts, state handling | FastAPI `/api/v1/issues` list/detail endpoints using existing Repository and Issue models, Pydantic response contracts, and React/TypeScript list/detail views using versioned API contracts. Backend Dockerfile now packages the committed fixture (`COPY fixtures ./fixtures`) so the API image imports without host bind mounts. | Focused frontend tests cover loading, list success, empty, API error, selection, detail, retry, and detail not found (7 passed). Focused API tests cover list, detail, empty, not found, and stable database error responses. PostgreSQL/Compose-backed backend suite passed: 55 passed, 0 failed. GitHub Actions passed for commit `5f227a7`. | This traceability row and roadmap Current Project State now record the completed implementation and evidence; API and UI contracts live in backend and frontend source. | Docker Compose demo imported 100 fixture issues; issue list displayed all 100 imported issues; selecting issue #5676 displayed its title, state, repository, repository source, issue source, timestamps, and body. Jesse explicitly approved Milestone 1.3 as complete. | Complete |
| R1-04 | Run deterministic `classify → retrieve fixture evidence → assess → propose → human review` triage stages with visible workflow status. | C: explicit stages and status; A: evidence distinction | Deterministic, keyword-rule-based `classify`, `retrieve_fixture_evidence`, `assess`, `propose`, and `human_review` stages reusing the existing Analysis/Recommendation/AuditEvent models (no migration); synchronous execution persists an audit-backed queued/running/completed/failed lifecycle (see ADR 0004); a completed run only ever produces a proposal, never a human decision (see ADR 0003); versioned `POST`/`GET /api/v1/issues/{issue_id}/triage` contracts restricted by CORS to the approved frontend origin; React triage action with separated Evidence, System inference, Proposed action, and Human review status sections. | All five stages execute in exact order; audit-backed lifecycle proven queued → running → completed on success and queued → running → failed on forced failure; Recommendation remains proposed with no HumanDecision created. PostgreSQL triage tests: 26 passed. Frontend suite: 11 passed. Focused CORS tests: 3 passed. GitHub Actions passed for commit `e75f295`. | This traceability row and roadmap Current Project State record the completed implementation and evidence; API and UI contracts live in backend and frontend source. | Visible browser demo showed separated Evidence, System inference, Proposed action, and Human review status sections; issue #5676 produced a deterministic closed-needs-verification classification, low severity, and a proposed verification action. Jesse explicitly approved Milestone 1.4 and R1-04 as complete. | Complete |
| R1-05 | Require an authorized local reviewer to approve, reject, or request revision; never infer a final decision from model output. | C: human approval gate; G: authorization; G3 | `POST /api/v1/issues/{issue_id}/triage/decision` reuses the existing HumanDecision/Recommendation/AuditEvent models (no migration); accepts only approve/reject/request_revision; requires the recommendation to still be `proposed`; records reviewer identity (fixed local single-user identifier; no authentication/RBAC, documented limitation per ADR 0005), optional bounded rationale, and an AuditEvent; React Human review section adds Approve/Reject/Request revision controls that disable after a recorded decision. | Determinism and safety tests pass: triage completion still leaves the recommendation proposed with no HumanDecision; each decision is created only via the explicit API; invalid decisions and missing recommendations are rejected; a second decision cannot overwrite the first (409); decisions cannot attach across unrelated issues; the corresponding AuditEvent is created; retrieval exposes the decision accurately. Focused backend suite: 37 passed. Complete backend suite: 103 passed against an isolated, disposable PostgreSQL test database. Frontend suite: 13 passed. GitHub Actions passed for commit `92663e5`. | This traceability row and roadmap Current Project State record the completed implementation and evidence; API and UI contracts live in backend and frontend source. | Visible browser demo proved a deterministic recommendation initially remained unapproved, displayed Approve/Reject/Request revision controls, and changed only after Jesse explicitly clicked Approve. Jesse explicitly approved Milestone 1.5 as complete after the ownership checkpoint. | Complete |
| R1-06 | Verify a fresh local run completes import, browse, analyze, review, and decision without a paid provider. | A: end-to-end workflow; C: workflow; G1 and G2 | `backend/tests/test_release1_acceptance.py` exercises the full workflow (offline fixture import, issue list/detail, deterministic triage, review, and an explicit approve decision) end to end through the same FastAPI app and models used in production, with no paid AI provider or network call anywhere in the path. README documents both the automated command and the manual browser walkthrough. | Acceptance test: 1 passed. Complete backend suite: 104 passed, 0 failed, 0 skipped against an isolated, disposable PostgreSQL test database (not the demo database, which remained exactly 100 fixture issues). Ruff formatting/lint and Compose configuration passed. GitHub Actions passed for commit `4d41f8a`. | This traceability row and roadmap Current Project State record the completed implementation and evidence. | A fresh isolated-database run completed import → browse → deterministic triage → review → explicit human decision, and the visible browser workflow was demonstrated. Jesse explicitly approved Milestone 1.6 and Release 1 as complete. | Complete |
| R1-07 | Establish Release 1 evidence and complete the ownership review. | K: communication and personal understanding; G12 | Evidence capture checklist and ownership walkthrough preparation. | Rubric audit and oral walkthrough checkpoint. | README, evidence index, and ownership notes. | Explain the workflow, tradeoffs, failure mode, and proof. | Not Started |
| R2-01 | Execute the deterministic triage workflow durably: persisted stage boundaries/status, bounded retry and timeout, idempotent requests, and safe recovery/resumption after interruption. | C: async workflow status; C: retry/timeout/idempotency/recoverable failure | [ADR 0007](adr/0007-durable-async-workflow-technology.md) selects Celery (worker) + Redis (broker/result backend), superseding deferred [ADR 0004](adr/0004-defer-async-workflow-technology-selection.md); PostgreSQL remains the sole canonical workflow-state store via migration `20260914_0002` (adds `analyses.idempotency_key`/`current_stage`/`attempt_count` and the `stage_attempts` table) plus the additive migration `20260914_0003` (adds `stage_attempts.output`, persisting each stage's deterministic result, and a database-level `uq_stage_attempts_analysis_stage_attempt` uniqueness constraint — kept as its own migration, not an edit to the already-applied `0002`, so it applies safely to both a fresh database and one that had already migrated). The five stages run in a Celery task; each stage checks its persisted output before executing, so a redelivered task resumes at the first incomplete stage instead of re-running or duplicating completed `StageAttempt`/`Recommendation`/`HumanDecision` rows. Bounded retry uses genuine `self.retry()`-driven Celery broker requeue (not an in-task sleep loop) with a persisted `retrying` state; a stage timeout produces `timed_out`. `POST /api/v1/issues/{issue_id}/triage` returns `202 Accepted` with an analysis id/status/poll URL; the `GET` status endpoint exposes `current_stage`, `attempt_count`, and stage-attempt history. `Idempotency-Key` deduplicates same-issue requests and rejects cross-issue reuse (409). The React UI polls with a bounded 30-second automatic cap; reaching the cap while the workflow is still non-terminal pauses automatic polling without re-enabling "Run deterministic triage", offering a GET-only "Refresh status" action that never starts a second workflow. The worker never creates a `HumanDecision`; the explicit Approve/Reject/Request revision gate is unchanged. | Final isolated-PostgreSQL backend verification: 113 passed, 0 failed, 0 skipped (includes per-stage resume, duplicate-prevention, and database-uniqueness-constraint tests); migration upgrade/downgrade/upgrade validated across 0001→0002→0003. Ruff format/lint clean. Frontend suite: 17 passed (adds the polling-cap pause/"Refresh status" regression test), lint clean, build clean. A real, non-eager Celery/Redis worker demonstrated genuine broker-mediated retry (`self.retry()` produced independent task redeliveries, not eager-mode recursion) and separately recovered from a genuinely killed worker: four accidentally-queued analyses for issue #5679 drained to completion with exactly one recommendation and no duplicate stage attempts or human decisions per analysis. The demo database remained exactly 100 fixture issues throughout. | This traceability row and roadmap Current Project State record the completed implementation and evidence; ADR 0007 records the technology decision and alternatives; migration `20260914_0003` documents why the durability correction was kept additive. | Visible browser demo showed the queued/running lifecycle resolve automatically to a completed result with working human-review controls, and confirmed "Run deterministic triage" stays disabled through an in-progress workflow. Jesse explicitly reviewed and approved Milestone 2.1 and R2-01 as complete. | Complete |
| R2-02 | Add a provider-neutral AI gateway that supplements (never replaces) the deterministic recommendation: neutral request/response/usage/error contracts, a deterministic mock adapter, one real provider adapter, an explainable routing decision, controlled fallback on provider failure, and token/estimated-cost recording. | D: provider-neutral contracts; D: mock + real adapter; D: routing decision; D: controlled fallback; D: token/cost recording | [ADR 0008](adr/0008-provider-neutral-ai-gateway.md) adds `app/ai_gateway/` (contracts, mock adapter, OpenAI adapter built on the existing `httpx` dependency, and an explainable router with in-stage fallback) and wires it into the existing Celery workflow (ADR 0007) as an additive `ai_inference` stage between `propose` and `human_review`. Reuses `stage_attempts.output`, `Recommendation.content`, and `AuditEvent.metadata_`; no database migration. `AI_PROVIDER` defaults to `mock` (zero network, zero cost) everywhere; OpenAI is opt-in via `AI_PROVIDER=openai` plus `OPENAI_API_KEY`, and the worker/API refuse to start with that combination missing the key rather than silently falling back. A provider timeout, HTTP error, or malformed/missing response falls back to the mock adapter within the same stage attempt (no extra Celery retry spent); estimated cost is computed from configurable per-million-token price settings. | Focused backend suite: 26 new tests (23 in `test_ai_gateway.py`, 3 in `test_workflow_tasks.py`) covering contract validation, mock determinism/zero-cost, OpenAI success/timeout/HTTP-error/malformed-response (all via mocked HTTP, no real network call), routing/fallback/configuration-error distinction, persisted AI-stage output exposed through the API, a routing `AuditEvent` per fresh attempt, and — the key cost-safety property — that a redelivered/resumed workflow never calls the provider again once the `ai_inference` stage has succeeded. Complete backend suite: 139 passed against an isolated, disposable PostgreSQL database; ruff format/lint clean. Frontend suite: 18 passed (adds a dedicated AI-inference-section/fallback-provenance test); lint and build clean. Compose configuration validated. | This traceability row, roadmap Current Project State, and [ADR 0008](adr/0008-provider-neutral-ai-gateway.md) record the implementation, evidence, and alternatives considered. | A visible browser demo showed the separate AI-generated inference section, `Provider: mock (deterministic-v1)`, unchanged deterministic classification/severity/proposed-action results, and intact human-review controls, with zero external/paid AI calls made. Jesse explicitly reviewed the demo and approved Milestone 2.2 and R2-02 as complete. | Complete |
| R2-03 | Add repository-grounded RAG: ingest resolved issues and a bounded documentation fixture, chunk and embed with pgvector, enforce repository/tenant scope, return stable metadata and source links, bound context size, and require the AI narrative to cite retrieved evidence. | E: chunked/embedded/stored with pgvector; E: scoped deterministic metadata/links; E: recommendations cite retrieved evidence; E: bounded/authorized context | [ADR 0009](adr/0009-repository-grounded-pgvector-retrieval.md) swaps the database image to `pgvector/pgvector:0.8.6-pg17-bookworm` (verified safe against the existing demo volume: same PostgreSQL major version, matching collation, all data intact) and adds the additive migration `20260916_0004` (`repository_documents`, `retrieval_chunks` with a `vector(384)` column; does not edit 0001-0003). `app/retrieval/` provides deterministic chunking, an embedding adapter interface, a real local CPU-only embedder (`fastembed` 0.8.0 + `BAAI/bge-small-en-v1.5`, MIT licensed, baked into the Docker image at build time — no paid API, no runtime network call) and a deterministic fake adapter for tests. A new `retrieve_related_evidence` Celery stage runs between `propose` and `ai_inference` through the existing `_run_stage`, inheriting Milestone 2.1's persisted-output/resume/no-duplicate-call guarantees; only its selected results (repository-scoped, analyzed-issue-excluded, top-3 as a cap not a quota — never backfilled) reach `AIRequest.retrieved_context`. The AI narrative cites only supplied identifiers; an invented citation is a controlled provider failure handled by the existing fallback path. A new documentation fixture (6 original, attributed Flask-docs paraphrases with real source URLs) is ingested alongside the 100 resolved issues via an idempotent `python -m app.retrieval` command. The retrieval query is built from issue-specific content only (title, bounded body excerpt, deduplicated matched keywords) — generic classification/severity/rationale/proposed-action boilerplate is excluded from the embedded query (still available unchanged to `ai_inference`) because it was found to homogenize similarity scores and defeat any threshold. Acceptance uses a calibrated two-band confidence model: below `retrieval_min_similarity` (0.65) a candidate is rejected; from 0.65 up to `retrieval_high_confidence_similarity` (0.90, calibrated from confirmed near-duplicate #5755/#5756 at ~0.928) a candidate additionally needs a shared *discriminative* term with the query (corpus-generic boilerplate and general low-content English words excluded, see `app.retrieval.query_terms`); at or above 0.90 semantic score alone qualifies. This replaced an earlier, narrower margin after Jesse's visible demo surfaced a false positive (#5942); live-verifying the fix against the real demo corpus also caught and fixed two further latent false positives (#5804, #5836) before they reached a real demo. | Full backend suite: 217 passed against a fresh isolated pgvector PostgreSQL database (migration upgrade/downgrade/upgrade validated from an empty database across 0001→0004→0003→0004); ruff format/lint clean. Frontend: lint clean, 19 tests passed, build clean (no frontend code changed in the final correction). Compose configuration validated. Jesse's own visible demo run for issue #5755 (the actual persisted `Recommendation`, not a rehearsal) returned citations `["issue:5756", "issue:5863", "issue:6139"]` — #5756 at 0.928 (strong-confidence threshold alone), #5863 at 0.708 (shared discriminative term "security"), #6139 at 0.691 (see Limitations) — with #5942 correctly absent from citations, the AI prompt, and the retrieval audit evidence; deterministic classification/severity/proposed action unchanged; human-review controls intact; zero external/paid network calls (`provider: "mock"` in the persisted record, confirmed via container logs); demo database exactly 100 issues and 405 retrieval chunks throughout. | This traceability row and roadmap Current Project State record the final implementation, calibration, and correction evidence; [ADR 0009](adr/0009-repository-grounded-pgvector-retrieval.md) records the underlying architectural decision (pgvector as the vector store) and alternatives considered — its own threshold figures predate both post-implementation calibration corrections and are superseded by this row and `tests/calibration/bge_similarity_calibration.json`. | Live demo showed real semantic retrieval, structural citations, and an unchanged deterministic/human-review path. One honest, disclosed limitation: issue #6139 ("AI junk", a demo-corpus filler issue genuinely about security vulnerabilities) is accepted for #5755 via the shared term "security" — a different sense of "security" than #5755's URL-path name. Single-shared-discriminative-term lexical confirmation cannot fully resolve this kind of polysemy; this was disclosed to and accepted by Jesse rather than force-fixed. Jesse reviewed the visible demo and explicitly approved Milestone 2.3 and R2-03 as complete. | Complete |
| R2-04 | Add a prompt registry and traceability: stable prompt names, semantic versions and content hashes, draft/released/retired status, and record prompt, model, parameters, provider, and context identifiers with every result. | F: stable names/semantic versions/content hashes/release status; F: prompt version/provider/model/parameters/context identifiers recorded per result | [ADR 0010](adr/0010-prompt-registry.md) adds `app/ai_gateway/prompts.py`: a code-owned, provider-neutral prompt registry (no database table, no external service, no runtime version selector). `PromptVersion` is immutable and hash-pinned (`prompt_id`, semantic `version`, `draft`/`released`/`retired` `status`, canonical `template`, pinned `template_hash`); `__post_init__` recomputes the template's SHA-256 and raises if it does not match the pinned value, so a released prompt's content cannot be silently edited without a version bump — the failure happens at module import time. The first shared release is `triage_narrative@1.0.0`, `status=released` (the prior bare `PROMPT_NAME` constant was retired, not versioned, since `mock_adapter` and `openai_adapter` had previously built two independently-maintained prompt strings under it). `render_active_prompt` is the single deterministic renderer both adapters call; `router.py` renders once per call and passes the same `RenderedPrompt` to whichever adapter runs, including the mock-adapter fallback path, so mock and OpenAI are structurally guaranteed identical rendered text and provenance. Two distinct SHA-256 hashes are tracked: `prompt_template_hash` (the fixed instructions alone) and `rendered_prompt_hash` (the exact text for one specific request) — persisted identically in `Recommendation.content`, the `ai_inference_routing` `AuditEvent.metadata_`, and the `TriageAIInference` API schema, with no new migration. The "retrieved evidence is untrusted data, not instructions" framing lives inside the fixed, hash-pinned template itself, always present, not conditionally appended. | Full backend suite: 251 passed against a fresh isolated pgvector PostgreSQL database (no migration involved); ruff format/lint clean. Frontend: lint clean, 19 tests passed, build clean. Compose configuration validated. Focused tests prove registry integrity (unique keys, semantic-version and status validation, tamper-detection via hash mismatch), byte-for-byte deterministic rendering, that mock and OpenAI adapters resolve identical rendered text/provenance (OpenAI verified via mocked HTTP only, no real network call), that `Recommendation.content` and the `AuditEvent` carry identical prompt provenance, that the API exposes all five provenance fields, and that the typed inputs behind a historical `rendered_prompt_hash` are already fully reconstructable from persisted `Recommendation.content` alone (no new snapshot field needed) — reproducing the exact hash on replay. Jesse's own visible demo run for issue #5676 (the actual persisted `Recommendation`, not a rehearsal) showed `Provider: mock (deterministic-v1)` and `Prompt: triage_narrative@1.0.0` in the UI, retrieved evidence and citations still connected to the AI narrative, Evidence/System inference/AI-generated inference/Proposed action remaining distinguishable, and intact Approve/Reject/Request revision controls; a read-only database inspection confirmed identical prompt provenance in `Recommendation.content` and the `AuditEvent`, and a read-only script reproduced the exact `rendered_prompt_hash` from persisted data alone; zero external/paid network calls; demo database exactly 100 issues and 405 retrieval chunks throughout. | This traceability row, roadmap Current Project State, and [ADR 0010](adr/0010-prompt-registry.md) record the implementation, evidence, and alternatives considered. | Live demo showed the required `Prompt: triage_narrative@1.0.0` display alongside the existing `Provider: mock (deterministic-v1)` line, with no `HumanDecision` recorded for the demonstrated analysis. Jesse explicitly reviewed the demo and approved Milestone 2.4 and R2-04 as complete. | Complete |
| R2-05 | Add an evaluation and regression harness: a fixed evaluation set covering classification/retrieval hit rate/groundedness/citation correctness/usefulness/safety; one command producing machine-readable and human-readable reports; documented thresholds that block a release when results regress; and a demonstrated failing regression gate, using "behavior regression" terminology rather than population drift. | E: retrieval quality measured on a fixed evaluation set; F: fixed evaluation dataset/versioned fixtures; F: automatic evaluation with machine+human reports; F: regression thresholds block release; F: behavior-regression, not population-drift, terminology | [ADR 0011](adr/0011-evaluation-and-regression-harness.md) adds `app/evaluation/`, a deterministic, zero-cost, zero-network regression gate (`python -m app.evaluation`) with three explicitly separated sections: (A) deterministic triage — the real `classify`/`assess`/`propose` against a 19-case, versioned, checked-in fixture (`backend/tests/evaluation/fixed_cases.json`); (B) retrieval-policy regression — the real, current `app.retrieval.service._confidence_decision`/`_shared_discriminative_terms` selection functions replayed against previously measured, frozen real-BAAI/bge-small-en-v1.5 similarity scores and bounded real query/candidate text (`tests/calibration/bge_similarity_calibration.json`, extended additively — no score fabricated or re-measured) — the only section that says anything about retrieval-selection quality, named precisely (`retrieval_policy_expected_accept_rate`/`retrieval_policy_forbidden_rejection_rate`, never "retrieval precision"); (C) narrative/citation behavior — the real AI gateway with a hand-supplied retrieved context, explicitly distinct from stage B. A checked-in, reviewable baseline (`backend/tests/evaluation/baseline.json`) is never regenerated silently — only an explicit `--update-baseline` flag writes it, and only after the run itself passes; ordinary runs never create untracked candidate-report files. Blocking metrics use "no regression from baseline" (classification/severity/action agreement, required/forbidden citations, citation validity at 100%, both retrieval-policy rates at 100%, mock cost at exactly $0.00); `narrative_rule_violations`, latency, and token/cost totals are warning-only/observational. A one-line CI prerequisite was corrected: `.github/workflows/ci.yml`'s Postgres service image was switched to the pinned `pgvector/pgvector:0.8.6-pg17-bookworm` image `compose.yaml` already used (migration `20260916_0004` requires the `vector` extension the prior plain-Postgres CI image did not provide), and a new "Run evaluation regression gate" CI step runs the harness with the mock provider only. | Full backend suite: 309 passed against a fresh isolated pgvector PostgreSQL database (no new migration; `20260916_0004` applies cleanly against the corrected CI image); ruff format/lint clean; `git diff --check` clean; Compose configuration validated. Two independent, temporary, in-memory-only demonstrations (never a checked-in file, fully reverted afterward) each produced a named, exact blocking regression and a nonzero exit code: one degrading a deterministic action mapping (stage A), one reverting the real Milestone 2.3 false-positive fix so #5942/#5836 would be incorrectly accepted again (stage B) — proving the gate actually blocks, not merely reports. A clean, unmodified run exits 0 with all three sections passing. Zero real, paid OpenAI calls at any point. | This traceability row, roadmap Current Project State, and [ADR 0011](adr/0011-evaluation-and-regression-harness.md) record the implementation, evidence, and alternatives considered (no LLM-as-judge, no database-backed run history, no frontend dashboard, no enterprise drift monitoring). | Jesse personally ran `python -m app.evaluation` and confirmed: deterministic triage metrics passed; retrieval expected-accept rate 100%; retrieval forbidden-rejection rate 100%; citation validity 100%; mock cost $0.0000; 19/19 evaluation cases passed; issue #6139 visibly disclosed as a known nonblocking limitation; baseline comparison PASS; exit code 0. Jesse explicitly reviewed this run and approved Milestone 2.5 and R2-05 as complete. | Complete |
| R2-06 | Add AI guardrails and API security: treat issue text and retrieved documents as untrusted data, not executable instructions, with prompt-injection fixtures; validate structured outputs and fail closed on invalid/unsafe output; enforce tool/action allowlists preventing unauthorized external or state-changing operations; apply API rate limiting, input limits, and safe error handling; and exclude, load securely, document, and scan secrets in CI. | G: untrusted-data framing and injection tests; G: structured-output validation and fail-closed behavior; G: tool/action allowlists and denial tests; G: API rate limiting/input limits/safe error handling; G: secrets excluded from Git, scanned in CI | [ADR 0012](adr/0012-ai-guardrails-and-adversarial-evaluation.md) treats the current issue's own title/body and retrieved repository evidence as untrusted data via a new, hash-pinned `triage_narrative@1.1.0` prompt release with explicit `BEGIN`/`END` untrusted-data boundaries (the previous `triage_narrative@1.0.0` framed only retrieved evidence and remains registered, unmodified, for historical inspection) — documented as risk reduction, not proof of injection immunity. `AIResponse` gained a provider-neutral 4,000-character narrative bound and duplicate/over-count citation rejection, both routing through the existing deterministic mock-adapter fallback on violation. A new, versioned, hash-pinned redaction policy (`provider_input_redaction@1.0.0`, `app.ai_gateway.redaction`, structurally mirroring the prompt registry) matches exactly three high-confidence secret formats (OpenAI-style key, GitHub PAT, PEM private key) and redacts them from the rendered prompt, the outgoing provider payload, and model-influenced narrative/citations — never from the separately-persisted original evidence snapshot, which remains unredacted by design. The policy's ID/version/status/hash and match events are persisted identically alongside prompt provenance on every `AIResponse` path (mock, real-provider, fallback), and a historical analysis's exact `rendered_prompt_hash` remains reproducible via `get_redaction_policy` even after a newer policy version becomes active. A new, versioned 15-case adversarial fixture (`backend/tests/evaluation/adversarial_cases.json`) forms Section D of `python -m app.evaluation` — prompt injection, fabricated citations, malformed/empty/oversized provider output, secret redaction, resource exhaustion, the human-review boundary, and the absence of tool/action execution, every case deterministic and blocking, run through the real production code paths; no tool-execution surface exists anywhere in this codebase, so the "allowlist" requirement is satisfied by proving and regression-testing that absence rather than building policy code for a capability that does not exist. A Redis-backed rate limiter (`app.api.rate_limit`), atomically incrementing and expiring via a single Lua script, guards the two state-changing endpoints (start triage, record a human decision) with HTTP 429/`Retry-After` on a safe, flat error body; Redis unavailability is a documented, deliberate fail-open choice. Two previously-unbounded state-changing request fields gained conservative length bounds. Secrets scanning was already satisfied by the existing `gitleaks-action@v2` CI step (Milestone 0.2); no new scanning tool was added. | Full backend suite: 385 passed against isolated PostgreSQL and Redis (the live demo stack — 100 issues, 405 retrieval chunks, migration head `20260916_0004`, all five services healthy — untouched); ruff format/lint clean; `git diff --check` clean; Compose configuration validated. A demonstrated intentional guardrail regression (redaction bypassed) produced exit code 1, naming the exact failing case and metric; the restored clean run produced exit code 0 with the checked-in baseline byte-identical throughout. Dedicated tests prove: `1.0.0`/`1.1.0` prompt coexistence and untrusted-section boundaries; a forged closing marker cannot escape the boundary; narrative/citation bounds; redaction integration on mock and OpenAI paths with no false positives on ordinary code/Markdown; a synthetic-secret replay reproduces the exact historical `rendered_prompt_hash`, including after a hypothetical newer redaction-policy version is activated (with a negative control); all 15 Section D cases; rate-limiter atomicity (positive TTL on first request, no reset on later increments, self-healing of a forced no-TTL key, a unit-level atomic-script test); fail-open on a simulated Redis outage; independent route/client counters; GET polling unaffected; a rejected request creates no `Analysis` row; safe 422 on oversized request fields. Zero real, paid OpenAI calls at any point. | This traceability row, roadmap Current Project State, and [ADR 0012](adr/0012-ai-guardrails-and-adversarial-evaluation.md) record the implementation, evidence, and alternatives considered (no tool-allowlist policy engine for a nonexistent capability, no provider-specific structured-output/JSON mode, no broad enterprise DLP or generic `KEY=value` regex, no authentication/RBAC in this milestone, no security dashboard, no CI run against a real provider). | Jesse personally ran `python -m app.evaluation` with `AI_PROVIDER=mock`/`EMBEDDING_PROVIDER=fake` and confirmed: Section D adversarial guardrails 15/15 passed; prompt version `triage_narrative@1.1.0`; the secret-redaction case passed; a fabricated citation safely triggered fallback; malformed, empty, oversized, and timeout provider cases safely triggered fallback; prompt and redaction provenance survived fallback; no `HumanDecision` was created; no tool/action execution occurred; baseline comparison PASS; exit code 0. Jesse explicitly reviewed this run and approved Milestone 2.6 and R2-06 as complete. Explicitly disclosed, not claimed solved: deterministic fixtures do not prove any real LLM resists every semantic prompt-injection technique; redaction covers only the three documented high-confidence patterns; original imported evidence remains unredacted by design; rate limiting is IP-keyed and fails open during a Redis outage. | Complete |
| R2-07 | Verify a fresh Release 2 exit: evaluation thresholds pass, a provider failure recovers safely, an injection attempt fails safely, and human approval remains mandatory -- all four proven together, through the real production code paths, in one focused acceptance run plus a live ownership walkthrough. | A: end-to-end AI-assisted workflow; C: workflow; G3, G5, G6, and G7 | `backend/tests/test_release2_acceptance.py` (11 tests) exercises the real API/Celery-eager-workflow/AI-gateway/evaluation paths: Section A calls the real `python -m app.evaluation` entry point and inspects the structured report for Sections A-D; Section B drives a real simulated OpenAI timeout (`httpx.post` monkeypatched, never a real request) through the actual workflow and confirms a completed analysis with a `fallback` AI-inference status and full provenance; Section C proves current-issue and retrieved-context injection attempts stay data, a fabricated citation is rejected, and a synthetic secret is redacted from provider-bound/model-generated content while the original evidence snapshot legitimately remains unredacted by design; Section D proves the mandatory human-approval boundary, including the existing 409 conflict contract. A CI-portability defect discovered during this milestone (`adversarial_fixture_hash` computed from raw file bytes, which differ by CRLF/LF across Windows/Linux checkouts) was root-caused with exact computed hashes and corrected with `app.evaluation.canonical.canonical_json_hash`, applied to both evaluation fixture fingerprints; the resulting baseline diff changed only the two hash values, no metric or outcome. A separately diagnosed live-stack issue (stale `api`/`worker` Docker images built before `triage_narrative@1.1.0` existed) was resolved by rebuilding and recreating only those two services, leaving PostgreSQL, Redis, and the demo data untouched. | Full backend suite: 398 passed against isolated PostgreSQL and Redis (the live demo stack -- 100 issues, 405 retrieval chunks, migration head `20260916_0004`, all five services healthy -- untouched throughout); ruff format/lint clean; `git diff --check` clean; Compose configuration validated. An intentional, temporary in-memory redaction bypass made the acceptance test fail with the exact predicted assertion; reverted with zero net source diff; the full suite then passed again. GitHub Actions green for the corrected commit `997eddea7cdc9dfac3f99776536cfcdc3d33a964` (independently confirmed). Zero real, paid OpenAI calls at any point. | This traceability row, roadmap Current Project State, and [ADR 0012](adr/0012-ai-guardrails-and-adversarial-evaluation.md) record the implementation and evidence; no new ADR was needed for the CI-portability fix or the Docker-image refresh (neither changed an architectural decision). | Jesse's isolated ownership run: `pytest -q backend/tests/test_release2_acceptance.py` -- 11 passed, exit code 0; `python -m app.evaluation` -- exit code 0. Jesse's live browser run (after the stale `api`/`worker` images were refreshed) showed `Provider: mock (deterministic-v1)`, `Prompt: triage_narrative@1.1.0`, repository-grounded citations, deterministic evidence displayed separately from the AI-generated inference, no automatic `HumanDecision`, and working Approve/Reject/Request-revision controls. Jesse explicitly reviewed this evidence and approved Milestone 2.7, R2-07, and Release 2 as complete. Explicitly disclosed, not claimed solved: deterministic/mock adversarial tests prove this application's own guardrail boundaries, not universal real-model prompt-injection immunity; redaction covers only the three documented high-confidence patterns; the original imported evidence snapshot remains unredacted by design; the retrieval-policy evaluation remains a small, frozen sample retaining the accepted #6139 limitation; rate limiting is IP-keyed and fails open during a Redis outage. | Complete |
| R3-01 | Add a durable organization/tenant data model: at least two organizations, and every repository-owned record chain terminating at an enforced tenant boundary (Milestone 3.1 Slice 1 of 2). | H: at least two organizations | [ADR 0013](adr/0013-organization-tenant-data-model.md) and migration `20260917_0005` add the two deterministic organizations and convert `Repository.tenant_id` into a `NOT NULL`, `ON DELETE RESTRICT` foreign key. Its guarded downgrade refuses to destroy custom-organization data. Slice 2's migration `20260918_0006` removes the temporary default, so every new repository requires explicit ownership and a missing tenant fails closed. | The original Slice 1 focused suite passed 17/17 and the full backend suite passed 415/415. Final Milestone 3.1 verification passed 488/488, including clean migration upgrade/downgrade coverage and explicit-tenant constraint tests. | This traceability row, roadmap Current Project State, [ADR 0013](adr/0013-organization-tenant-data-model.md), and [ADR 0014](adr/0014-synthetic-identity-and-tenant-isolation.md) record the completed data-model foundation and its Slice 2 hardening. | Migrations `20260917_0005` and `20260918_0006` are active in the live demo. Exactly two organizations and two explicitly-owned repositories are present; all pre-0006 row IDs and counts were preserved through migration, the isolation organization contains only the approved synthetic fixture, and the verified pre-0006 backup is retained outside the repository. Jesse approved Slice 1 and the final Milestone 3.1 ownership walkthrough. | Complete |
| R3-02 | Add request identity, role-based authorization, and backend-enforced tenant scoping across every protected resource, retrieval path, and workflow stage (Milestone 3.1 Slice 2 of 2). | H: viewer/reviewer/administrator roles; H: backend-enforced permissions; H: cross-tenant negative tests covering records, vectors, analyses, and traces; G4 | [ADR 0014](adr/0014-synthetic-identity-and-tenant-isolation.md) adds the synthetic `X-Demo-Actor-ID` selector (explicitly not authentication), database-backed viewer/reviewer/administrator roles, centralized tenant-scoping helpers, `RetrievalTenantMismatchError`, migration `20260918_0006`, and durable `Analysis.initiating_actor_id`. The worker trusts only the analysis ID in its Celery payload, re-derives ownership from PostgreSQL, and invokes the same authorization guard at `authorize`, `authorize_before_retrieval`, `authorize_before_ai_inference`, and `authorize_before_persistence`. External AI calls are not transactionally enclosed; authorization is checked immediately before inference and before persistence. | Full backend suite: 488/488 passed against fresh disposable PostgreSQL/Redis; frontend suite: 28/28 passed. Tests cover missing/invalid/disabled identity, role denial, cross-tenant records/vectors/analyses/traces, explicit retrieval mismatch failure, all four workflow checkpoints, role/tenant changes between stages, crash/resume reauthorization, idempotency, and the mandatory human-decision boundary. Ruff format/check, frontend build, base and demo Compose validation, secrets scan, generated-artifact check, and `git diff --check` passed. | This traceability row, roadmap Current Project State, and [ADR 0014](adr/0014-synthetic-identity-and-tenant-isolation.md) record the completed implementation, evidence, residual limitations, and G4 closure. | Migration `20260918_0006` is active live with five deterministic actors and the approved synthetic isolation fixture. Live negative checks hid repositories, issues, analyses, recommendations, decisions, documents, chunks, attempts, and audit/provenance rows across tenants; retrieval returned only same-repository evidence; viewer actions were denied; reviewer approval was explicit and persisted; repeated triage was idempotent; and AI completion created no `HumanDecision`. Jesse's browser walkthrough confirmed viewer restrictions, reviewer approval with actor/timestamp, mock provider and prompt provenance, isolation visibility limited to synthetic issues #1–#3, and state clearing when identities changed. Jesse approved Slice 2, Critical Gate G4, and Milestone 3.1. No paid provider call occurred. Residual boundaries: synthetic identity is not authentication; rate limiting is IP-keyed and fail-open during Redis outages. | Complete |
| R3-03 | Enforce append-only audit history and capture imports, analyses/workflow, routing, and human decisions without weakening tenant isolation or retry idempotency. | H: append-only audit events capture imports, analyses, routing, approvals, rejections, edits, and administrative changes | [ADR 0015](adr/0015-database-enforced-append-only-audit-events.md) and migration `20260919_0007` add PostgreSQL row/statement triggers rejecting update/delete/truncate, stable restrictive parent foreign keys, and a guarded downgrade. Imports append `issues_imported` only when new issues are inserted; existing workflow, retrieval, routing, prompt/redaction provenance, and human-decision evidence remains unchanged. The application has no edit or administrative mutation feature yet, so those future event types are explicitly not claimed. | Recovery verification against fresh disposable PostgreSQL/Redis: 92 focused mutation, tenant, workflow/retry/idempotency, importer, human-decision, acceptance, and 0006→0007/migration-cycle tests passed; the complete backend suite passed 502/502. Removing both triggers made the direct-SQL mutation test fail with `DID NOT RAISE`; recreating them made it pass. Ruff checked 115 files and reported `115 files already formatted`; lint, compilation, diff, base/demo Compose, changed-line secret, and generated-artifact checks passed. GitHub Actions passed for implementation commit `dc9baef43a6e91ad3d9a0ce58bddf41ba11731be`. | Roadmap Current Project State and ADR 0015 document ordinary-DML enforcement, the local Compose owner/superuser limitation, the required future migration-owner/runtime-role separation, coverage gaps, and revisit conditions. | Live migration `20260919_0007` is active after a verified external custom-format backup. Counts and sorted-ID fingerprints for all 11 application tables were unchanged. Both triggers are enabled; both parent foreign keys are restrictive; rollback-safe live checks proved insert succeeds and update/delete/truncate/protected-parent deletion fail with zero demonstration residue. Tenant-scoped reads and all five services remained healthy under mock/local providers with no paid call. Jesse approved the live evidence and documented trust boundary. | Complete |
| R3-04 | Make one analysis diagnosable end to end across frontend request, API, workflow, retrieval, model routing/call, token/cost evidence, and audit history; add meaningful OpenTelemetry spans/errors, an operational view, and a simulated-failure runbook. | I: correlation IDs; I: OpenTelemetry spans/errors; I: operational latency/failure/retry/token/cost/evaluation reporting; I: health/readiness and structured logs; I: diagnostic runbook; G8 | [ADR 0016](adr/0016-open-telemetry-observability.md) and migration `20260920_0008` add validated/generated frontend/API correlation, durable W3C parent context on `Analysis`, linked worker deliveries, spans for HTTP/authorization/workflow stages/retrieval/AI/persistence/human decisions, bounded metrics for latency/outcomes/retries/retrieval/provider tokens/cost/evaluation, correlated append-only audit metadata, and JSON trace-aware logs. Compose adds one Collector feeding Jaeger and Prometheus; no custom dashboard or parallel telemetry stack. Export failure is fail-open for application behavior. | Final disposable verification passed the complete backend suite 507/507, including corrected explicit-revision 0007 tests and migration 0008 coverage; Ruff format/lint, clean migration upgrade, and the deterministic evaluation baseline passed. Frontend: 30/30, ESLint and production build clean. Base/demo Compose validation, `git diff --check`, and the changed/untracked-candidate Gitleaks scan passed. Implementation commit `ce2ebe5dece0cb56ebaadb647c7551d9b74d06d8` passed [GitHub Actions CI run 35361615260](https://github.com/JGM-droid/RepoTriage-AI/actions/runs/35361615260). | [ADR 0016](adr/0016-open-telemetry-observability.md), the [runbook](OBSERVABILITY_RUNBOOK.md), roadmap Current Project State, tests, and local Jaeger/Prometheus evidence document the design and proof. Telemetry explicitly excludes issue/retrieved/prompt/provider content, secrets, credentials, sensitive headers, and exception messages; metric labels exclude raw IDs. | Jesse approved the isolated ownership walkthrough, which connected one analysis across API response, JSON logs, API-to-worker Jaeger trace, tenant-scoped audit history, and Prometheus metrics. In the same disposable stack, a second analysis completed with the Collector stopped; durable audit evidence remained, expected exporter errors were logged, and the restored Collector target returned healthy. Missing outage telemetry was not replayed. The separate live demo remained read-only at `20260919_0007` with unchanged counts and five healthy mock/local services. Milestone 3.3 is complete and G8 is satisfied. | Complete |
| R0-01 | Maintain Release 0 project controls that govern Release 1 implementation. | B: ADRs; K: documentation; change-control rule | Control documents, ADR system, traceability matrix, and agent instructions. | Documentation consistency review and Markdown link/table checks. | This document, rubric, roadmap, ADRs, and project instructions. | Control-document walkthrough. | In Progress |
| R0-02 | Provide the Release 0.2 runnable skeleton with a typed frontend, versioned health/readiness API, PostgreSQL, Alembic setup, Compose, checks, and CI. | A: typed UI states; B: versioned API; I: health/readiness; J: tests, containers, CI; G1, G9, G11 | React/TypeScript Vite frontend; FastAPI/Pydantic API; SQLAlchemy engine/session; PostgreSQL Compose service; initial Alembic configuration; GitHub Actions. | Frontend loading/healthy/error/retry tests; API health/readiness tests; formatting, lint, build, Compose configuration, stack smoke checks, and successful GitHub Actions run for commit `dbca8ba`. | README setup guide, ADR 0001, roadmap state, and dependency manifests. | Frontend service state; health and readiness responses; Compose service health. | Complete |
